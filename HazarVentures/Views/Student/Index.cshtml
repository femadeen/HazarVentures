@model HazarVentures.ViewModels.ResponseModels.StudentsResponseModel

<style>
    a:hover {
        font-size: 80%;
    }
</style>

<h1 style="margin-top: 50px; margin-bottom:30px;">List of Students</h1>
<table class="table table-sm table-hover">
    <thead>
        <tr>
            <th style="font-weight: bolder; font-size: larger;">
                S/N
            </th>
            <th>
                Name
            </th>
            <th>
                Edit
            </th>
            <th>
                Details
            </th>
            <th>
                Delete
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model.Data)
        {
            <tr>
                <td>
                    @student.Id
                </td>
                <td>
                    @student.FirstName @student.LastName
                </td>
                <td>
                    <a asp-action="Update"asp-route-id="@student.Id">Edit</a>
                </td>
                <td>
                    <a asp-action="Details"asp-route-id="@student.Id">Details</a>
                </td>
                <td>
                    <a asp-action="DeleteStudent"asp-route-id="@student.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<p style="margin-top: 10px;"><button type="button" style="padding:4px 8px; color: white; border-radius: 8px; background-color:darkcyan;"><a asp-controller="Student" asp-action="CreateStudent" style="color:white; text-decoration:none;">Student Registration</a></button></p>
